# Antigravity Context
Apply the conventions defined in `AGENTS.md` in all your responses.
@AGENTS.md

Your custom slash commands are SKILL.md with frontmatter `name:` + `description:`
in `.agents/skills/<name>/SKILL.md` — invoke them as `/wf-onboard`,
`/wf-settings`, `/wf-worktree`, `/wf-ladder` when applicable.
