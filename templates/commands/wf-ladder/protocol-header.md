# Fragment: wf-ladder (OPTIONAL feature)

<!--
  SINGLE SOURCE of the wizard's own anti-over-engineering discipline (7 rungs). OPTIONAL
  feature: injected only if state.answers.decision_ladder == true (footer wf-ladder=yes).
  Renamed from templates/protocols/decision-ladder/ladder.md — same rungs, no logic change.
  This protocol has NO equivalent in gentle-ai and does not decide SDD/routing/delegation —
  see wf-sdd-trigger for that. Body follows gentle-ai's own docs/skill-style-guide.md structure.
  Consumed by: Builder (B3/B5), wf-refresh (optional features catalog), wf-settings (toggle).
-->
